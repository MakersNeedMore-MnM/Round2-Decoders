[project]
name = "round2-decoders-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.141.1",
    "pydantic-settings==2.14.2",
]

[tool.vercel]
entrypoint = "app.main:app"